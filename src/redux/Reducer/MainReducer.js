
const initialState ={
    products:[],
    basket:[],
}

export const MainReducer = (state = initialState, action) => {
    switch (action.type){





        default :
            return state
    }
}
